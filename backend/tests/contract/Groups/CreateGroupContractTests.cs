namespace FairShareApp.Backend.Tests.Contract.Groups;

public sealed class CreateGroupContractTests
{
    [Fact]
    public void PostGroups_ShouldReturn201WithGroupPayload()
    {
        // Placeholder contract test to be wired against API host in next iteration.
        Assert.True(true);
    }
}
